sudo bash -c 'chmod -x /etc/update-motd.d/*; rm -f /etc/motd.dynamic /run/motd.dynamic; : > /etc/motd; : > /etc/issue; sed -i "s|^PrintMotd yes|PrintMotd no|" /etc/ssh/sshd_config; sed -i "s|^#PrintMotd yes|PrintMotd no|" /etc/ssh/sshd_config; systemctl restart sshd'
